export const loadingdata = Array.from({ length: 8 }, (_, index) => ({
    key: String(index),
    value: `Item ${index + 1}`
}))

export const loadingdatas = Array.from({ length: 5 }, (_, index) => ({
    key: String(index),
    value: `Item ${index + 1}`
}))